{
  "name": "@bruce/events",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "retry-dlq": "tsx src/scripts/retry-dlq.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bruce/contracts": "workspace:*",
    "@bruce/logger": "workspace:*",
    "@bruce/stripe-client": "workspace:*",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.0",
    "prom-client": "^15.1.3",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  }
}
