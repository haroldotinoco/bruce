{
  "name": "@bruce/agent-runtime",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bruce/events": "workspace:*",
    "@bruce/llm": "workspace:*",
    "@bruce/logger": "workspace:*",
    "@bruce/project-store": "workspace:*",
    "@temporalio/activity": "^1.16.0",
    "@bruce/redis": "workspace:*",
    "@bruce/storage": "workspace:*",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  }
}
