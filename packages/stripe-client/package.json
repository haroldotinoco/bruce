{
  "name": "@bruce/stripe-client",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bruce/auth": "workspace:*",
    "@bruce/db": "workspace:*",
    "@bruce/logger": "workspace:*",
    "drizzle-orm": "^0.29.0",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  }
}
